# Deployment requirements (no torch — too large for Render free tier)
# Models are pre-trained and shipped as .pkl files

fastapi>=0.111
uvicorn[standard]>=0.29
python-dotenv>=1.0
requests>=2.31
websockets>=12
pandas>=2.0
numpy<2
catboost>=1.2
xgboost>=2.0
lightgbm>=4.0
scikit-learn>=1.4
arch>=6.3
pmdarima>=2.0
statsmodels>=0.14
pytz>=2024.1
scipy>=1.10
pyotp>=2.9

# Nubra SDK
# Install separately:
# pip install --index-url https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple nubra-sdk
